# Agent hero demo: the claude preset's posture (mask .env*, allow only Anthropic), trimmed for a
# fast deterministic recording - no claude-code npm and no live model call. The on-camera `sluice
# agent` list still shows the real presets; this box just proves the cage.
SLUICE_DESC="Claude Code (Anthropic)"
SLUICE_ALLOW_DOMAINS="api.anthropic.com"
SLUICE_MASK=".env*"
SLUICE_ENV="ANTHROPIC_API_KEY"
SLUICE_RUN_CMD="true"
